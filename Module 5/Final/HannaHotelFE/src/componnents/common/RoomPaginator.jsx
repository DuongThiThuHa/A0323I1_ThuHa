import React from 'react'

const RoomPaginator = ({}) => {
    const pageNumbers = Array.from({ length: totalPages, onPage })
    return ( <
        div >

        <
        /div>
    )
}

export default RoomPaginator